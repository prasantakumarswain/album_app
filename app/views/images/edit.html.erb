<div>
  <%= form_for @image, :url => images_path, :html => { :multipart => true } do |f| %>
      <%= f.label  :name %>
      <%= f.text_field  :name %>
      <%= f.file_field :avatar %>

      <div>
        <%= f.submit "update" %>
      </div>
  <% end %>
</div>